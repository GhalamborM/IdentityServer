// <auto-generated by="ValueObjectsGenerator"/>
// Copyright (c) Duende Software. All rights reserved.
// See LICENSE in the project root for license information.
#nullable enable

using System.Collections.Generic;
using System.Diagnostics.CodeAnalysis;

namespace Duende.UserManagement;

[System.ComponentModel.TypeConverter(typeof(ValueOfTypeConverter<PhoneNumber, string>))]
partial record PhoneNumber : IStringValue<PhoneNumber>
{
    // Constructor for controlled creation
    private PhoneNumber(string value) => Value = value;

    /// <summary>Gets the underlying string value of this <see cref="PhoneNumber"/>.</summary>
    public string Value { get; }

    /// <summary>Creates a <see cref="PhoneNumber" /> from the specified string value.</summary>
    /// <param name="s">The string value.</param>
    /// <returns>A new <see cref="PhoneNumber" /> instance.</returns>
    /// <exception cref="FormatException">Thrown when <paramref name="s" /> is not a valid <see cref="PhoneNumber" />.</exception>
    public static PhoneNumber Create(string s)
    {
        if (!TryCreate(s, out var result, out var errors))
        {
            throw new FormatException($"The value '{s}' is not a valid PhoneNumber. {string.Join(" ", errors)}");
        }
        return result;
    }

    /// <summary>Tries to create a <see cref="PhoneNumber" /> from the specified string value.</summary>
    /// <param name="s">The string value.</param>
    /// <param name="result">When this method returns <see langword="true" />, contains the created <see cref="PhoneNumber" />.</param>
    /// <returns><see langword="true" /> if the value is valid; otherwise, <see langword="false" />.</returns>
    public static bool TryCreate(string? s, [NotNullWhen(true)] out PhoneNumber? result)
        => TryCreate(s, out result, out _);

    /// <summary>Tries to create a <see cref="PhoneNumber" /> from the specified string value.</summary>
    /// <param name="s">The string value.</param>
    /// <param name="result">When this method returns <see langword="true" />, contains the created <see cref="PhoneNumber" />.</param>
    /// <param name="errors">When this method returns <see langword="false" />, contains the validation errors.</param>
    /// <returns><see langword="true" /> if the value is valid; otherwise, <see langword="false" />.</returns>
    public static bool TryCreate(string? s, [NotNullWhen(true)] out PhoneNumber? result, [NotNullWhen(false)] out IReadOnlyList<string>? errors)
    {
        result = null;
        errors = null;
        if (string.IsNullOrWhiteSpace(s))
        {
            errors = ["A value is required."];
            return false;
        }

        s = Normalize(s);
        if (string.IsNullOrWhiteSpace(s))
        {
            errors = ["Value is empty after normalization."];
            return false;
        }
        var validationErrors = new List<string>();
        if (s.Length > MaxLength)
        {
            validationErrors.Add($"Must not exceed {MaxLength} characters.");
        }
        if (!TryValidate(s, out var tryValidateErrors))
        {
            if (tryValidateErrors is { Count: > 0 })
            {
                validationErrors.AddRange(tryValidateErrors);
            }
            else
            {
                validationErrors.Add($"The value '{s}' is not valid.");
            }
        }
        if (validationErrors.Count > 0)
        {
            errors = validationErrors;
            return false;
        }
        result = new PhoneNumber(s);
        return true;
    }

    /// <summary>Implicitly converts a <see cref="string"/> to a <see cref="PhoneNumber"/>.</summary>
    /// <param name="value">The string value to convert.</param>
    /// <returns>A new <see cref="PhoneNumber"/> instance.</returns>
    public static implicit operator PhoneNumber(string value) => Create(value);

    /// <inheritdoc />
    public override string ToString() => Value;

    /// <summary>Creates a new <see cref="PhoneNumber"/> from the specified string value, or returns <see langword="null"/> if the input is null or empty.</summary>
    /// <param name="input">The string value to create the <see cref="PhoneNumber"/> from.</param>
    /// <returns>A new <see cref="PhoneNumber"/> instance, or <see langword="null"/> if <paramref name="input"/> is null or empty.</returns>
    public static PhoneNumber? CreateOrDefault(string? input)
    {
        if (string.IsNullOrEmpty(input))
        {
            return null;
        }

        return Create(input);
    }

    internal static PhoneNumber Load(string value) => new PhoneNumber(value);

}
