<Project>
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

  <PropertyGroup>
    <!-- Samples are console apps with Console.WriteLine; localization is not needed -->
    <NoWarn>$(NoWarn);CA1303</NoWarn>
  </PropertyGroup>
</Project>